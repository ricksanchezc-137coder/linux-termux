Módulo 1: Shell avançado (variáveis, expansão, quoting, subshells)

- Variáveis de shell vs. variáveis de ambiente (export)
- Expansão de parâmetros: ${var}, defaults com :- e :=
- Quoting: aspas simples, duplas e sem aspas; word-splitting
- Subshells (parênteses) vs. execução no shell atual (chaves)

Observação de ambiente: /tmp não existe no Termux, usado $PREFIX no lugar.
